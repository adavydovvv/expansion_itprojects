&НаКлиенте
Процедура ПередЗаписью(Отказ, ПараметрыЗаписи)
	
	Если Объект.СредняяЗанятость > 100 Тогда
		Отказ = Истина;
		ПоказатьПредупреждение(, "Нормативная загрузка не может превышать 100%");
	КонецЕсли;
	
КонецПроцедуры
