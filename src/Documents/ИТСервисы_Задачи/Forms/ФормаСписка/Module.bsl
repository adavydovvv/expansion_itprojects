&НаСервере
Процедура ЗапуститьСинхронизациюНаСервере()
	ИТСервисы_ИнтеграцияKaitenСервер.ЗапуститьСинхронизациюНаСервере();
КонецПроцедуры

&НаКлиенте
Процедура ЗапуститьСинхронизацию(Команда)
	Попытка
		ЗапуститьСинхронизациюНаСервере();
		Сообщить("Команда синхронизации отправлена.");
	Исключение
		Сообщить("Ошибка: " + ОписаниеОшибки());
	КонецПопытки;
КонецПроцедуры
